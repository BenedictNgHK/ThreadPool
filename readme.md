# ThreadPool

Modern C++ implementation of work-stealing Threadpool, supporting variadic function and retrieving return value. Using CMake to build.

## Build:
```sh
cd build
cmake ..
make
```

Output executable is located in `bin` folder. The library is in `lib` folder.

